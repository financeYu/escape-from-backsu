interface:
  display_name: "Agent Worker Pool"
  short_description: "Normalize bounded coder, validator, reporter, and tracker packets."
  default_prompt: "Use agent-worker-pool to validate and normalize supervisor worker packets without executing worker tasks."
