# Valuation / Fundamental Context

Canonical references:

- `docs/technical_valuation_boundary.md`
- `docs/step2_financial_validation_summary.md`
- Step 18 boundary docs after Step 18 integration.

Boundary summary:

- Valuation/fundamental data must stay out of `technical_composite_score` and `final_composite_score` unless a future approved Step changes that policy explicitly.
- Price-only evidence must not be called cheap, value, undervalued, or bargain.
- Point-in-time availability must remain explicit; `collected_at` alone is not enough.
- This Pre-Step19 task changes no valuation scoring logic.
