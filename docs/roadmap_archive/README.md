# Roadmap Archive

This directory keeps detailed completed-Step history that no longer belongs in the compact current roadmap status.

Use `docs/roadmap_status.md` for active task routing and current guardrails. Use archived files only when a completed Step artifact is directly touched, when a hard-stop question depends on the old record, or when the user explicitly asks for historical Step-end evidence.

